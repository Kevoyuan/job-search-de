# No candidate profile is stored in this skill

Candidate-specific facts belong in `<workdir>/.job-search/profile.md`.

Run:

```bash
python3 scripts/init_config.py --workdir <workdir>
```

Then replace the generated placeholders with verified candidate facts. See `references/configuration.md` for the contract.
